server:
  port: 8999
spring:
  application:
    name: auth-service
  messages:
    encoding: UTF-8
    basename: i18n/messages_common
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    #MySQL配置
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-0.mysql.mysql.svc.cluster.local:3306/sys-threec?useSSL=false&serverTimezone=UTC
    username: root
    password: MSStarForever*00
logging:
  level:
    root: info
    org:
      springframework:
        security: DEBUG
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.threec.entity
  global-config:
    db-config:
      logic-delete-field: delFlag  # 全局逻辑删除字段名
      logic-delete-value: 1        # 逻辑已删除值
      logic-not-delete-value: 0    # 逻辑未删除值
      id-type: auto
application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days
    b-crypt-password-encoder:
      strength: 11
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true